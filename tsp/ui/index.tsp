/**
 * UI Module
 *
 * Re-exports all presentation-layer value object models.
 * These types are used by web UI components and are kept
 * separate from domain types per Clean Architecture boundaries.
 *
 * @module ui
 */
import "./prd-questionnaire.tsp";
