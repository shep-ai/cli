# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: fix-copy-worktree-path
number: 071
branch: feat/071-fix-copy-worktree-path
oneLiner: We clicking on an exsiting feature action "copy path"
the path that is copied is the repo path instead of the worktree path.
when I click on a feature node
feature drawer is opened
in the feature drawer clciking on the action of copy path should copy the work tree path
userQuery: >
  We clicking on an exsiting feature action "copy path"
the path that is copied is the repo path instead of the worktree path.
when I click on a feature node
feature drawer is opened
in the feature drawer clciking on the action of copy path should copy the work tree path
summary: >
  We clicking on an exsiting feature action "copy path"
the path that is copied is the repo path instead of the worktree path.
when I click on a feature node
feature drawer is opened
in the feature drawer clciking on the action of copy path should copy the work tree path
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures:
  []

technologies:
  []

relatedLinks:
  []

# Open questions (must be resolved before implementation)
openQuestions:
  []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  We clicking on an exsiting feature action "copy path"
the path that is copied is the repo path instead of the worktree path.
when I click on a feature node
feature drawer is opened
in the feature drawer clciking on the action of copy path should copy the work tree path

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
