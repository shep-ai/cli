# Task Breakdown (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: "interactive-feature-agent"
summary: >
  10 tasks across 5 phases. Phase 1: executor interface + ClaudeCode implementation (3 tasks).
  Phase 2: use cases + API route updates (2 tasks). Phase 3: session service refactoring (2 tasks).
  Phase 4: dead code cleanup (1 task). Phase 5: test updates + verification (2 tasks).

relatedFeatures:
  - "074-interactive-feature-agent (iteration 1)"
technologies:
  - "@anthropic-ai/claude-agent-sdk"
  - "TypeScript"
  - "tsyringe"
  - "Vitest"
relatedLinks:
  - title: "@anthropic-ai/claude-agent-sdk npm"
    url: "https://www.npmjs.com/package/@anthropic-ai/claude-agent-sdk"

tasks:
  # ─────────────────────────────────────────────────────────────
  # PHASE 1 — Executor Pattern Extension
  # ─────────────────────────────────────────────────────────────

  - id: "task-1"
    phaseId: "phase-1"
    title: "Create IInteractiveAgentExecutor interface with InteractiveAgentSessionHandle"
    description: >
      Create packages/core/src/application/ports/output/agents/interactive-agent-executor.interface.ts.

      Define three interfaces and one type:
      - InteractiveAgentOptions { cwd: string; model?: string; systemPrompt?: string }
      - InteractiveAgentEvent { type: 'delta'|'tool_use'|'tool_result'|'status'|'done'|'error'; content?: string; label?: string; detail?: string }
      - InteractiveAgentSessionHandle { sessionId: string; send(msg): void; stream(): AsyncIterable<InteractiveAgentEvent>; close(): Promise<void> }
      - IInteractiveAgentExecutor { createSession(opts): Promise<Handle>; resumeSession(id, opts): Promise<Handle> }

      Export from the agents/ index.ts barrel.
    state: "Todo"
    dependencies: []
    acceptanceCriteria:
      - "Interface file exists with all four type definitions"
      - "Exported from agents/index.ts"
      - "pnpm build succeeds"
    tdd: null
    estimatedEffort: "S"

  - id: "task-2"
    phaseId: "phase-1"
    title: "Extend IAgentExecutorFactory with createInteractiveExecutor()"
    description: >
      Add two new methods to IAgentExecutorFactory interface:
      - createInteractiveExecutor(agentType: AgentType, authConfig: AgentConfig): IInteractiveAgentExecutor
      - supportsInteractive(agentType: AgentType): boolean

      Update the AgentExecutorFactory implementation to support these methods.
      For now, only ClaudeCode returns an interactive executor; others throw
      'Agent type X does not support interactive mode'.
    state: "Todo"
    dependencies:
      - "task-1"
    acceptanceCriteria:
      - "IAgentExecutorFactory interface has createInteractiveExecutor() and supportsInteractive()"
      - "AgentExecutorFactory implementation handles Claude Code type"
      - "Other agent types throw descriptive error for createInteractiveExecutor()"
      - "supportsInteractive() returns true only for ClaudeCode"
      - "pnpm build succeeds"
    tdd:
      red:
        - "Test: factory.supportsInteractive(ClaudeCode) returns true"
        - "Test: factory.supportsInteractive(Cursor) returns false"
        - "Test: factory.createInteractiveExecutor(unsupported) throws"
      green:
        - "Implement methods in AgentExecutorFactory"
      refactor:
        - "Ensure consistent error message pattern with existing createExecutor()"
    estimatedEffort: "M"

  - id: "task-3"
    phaseId: "phase-1"
    title: "Implement ClaudeCodeInteractiveExecutor using SDK"
    description: >
      Create packages/core/src/infrastructure/services/agents/common/executors/claude-code-interactive-executor.service.ts.

      This is the ONLY file importing from @anthropic-ai/claude-agent-sdk. Implements
      IInteractiveAgentExecutor by:
      - createSession(): calls unstable_v2_createSession() with SDK options mapped from
        InteractiveAgentOptions (model, cwd, systemPrompt as preset+append, permissionMode,
        includePartialMessages). Strips CLAUDECODE env var.
      - resumeSession(): calls unstable_v2_resumeSession() with sessionId.
      - Returns an InteractiveAgentSessionHandle that wraps the SDK session:
        - send() → sdkSession.send()
        - stream() → async generator that maps SDK message types to InteractiveAgentEvent
        - close() → sdkSession.close()
        - sessionId → sdkSession.sessionId

      SDK message mapping happens INSIDE the executor's stream() implementation:
        SDKPartialAssistantMessage → { type: 'delta', content: extracted text }
        SDKAssistantMessage with tool_use blocks → { type: 'tool_use', label, detail }
        SDKResultMessage success → { type: 'done', content: result text }
        SDKResultMessage error → { type: 'error', content: error message }
        SDKStatusMessage → { type: 'status', content: status text }
        SDKToolUseSummaryMessage → { type: 'tool_result', label, detail }
        SDKToolProgressMessage → { type: 'status', label, detail }

      Register in AgentExecutorFactory (from task-2).
    state: "Todo"
    dependencies:
      - "task-2"
    acceptanceCriteria:
      - "ClaudeCodeInteractiveExecutor implements IInteractiveAgentExecutor"
      - "Only file importing from @anthropic-ai/claude-agent-sdk"
      - "createSession maps InteractiveAgentOptions to SDK options correctly"
      - "stream() maps all SDK message types to InteractiveAgentEvent"
      - "handle.close() calls sdkSession.close()"
      - "CLAUDECODE env var stripped in session creation"
      - "systemPrompt uses preset: 'claude_code' with append"
      - "Registered in AgentExecutorFactory"
      - "pnpm build succeeds"
    tdd:
      red:
        - "Test: createSession maps cwd, model, systemPrompt to SDK options"
        - "Test: stream() maps SDKPartialAssistantMessage to delta event"
        - "Test: stream() maps SDKResultMessage success to done event"
        - "Test: stream() maps SDKResultMessage error to error event"
        - "Test: close() calls sdkSession.close()"
      green:
        - "Implement executor with SDK calls and message mapping"
      refactor:
        - "Extract SDK message mapper into a private method"
    estimatedEffort: "L"

  # ─────────────────────────────────────────────────────────────
  # PHASE 2 — Use Cases & API Route Updates
  # ─────────────────────────────────────────────────────────────

  - id: "task-4"
    phaseId: "phase-2"
    title: "Create interactive session use cases"
    description: >
      Create four use case classes under packages/core/src/application/use-cases/interactive/:

      1. StartInteractiveSessionUseCase
         @injectable() with @inject('IInteractiveSessionService')
         Input: { featureId, worktreePath, agentType?, model? }
         Delegates to service.startSession(featureId, worktreePath, agentType, model)

      2. SendInteractiveMessageUseCase
         @injectable() with @inject('IInteractiveSessionService')
         Input: { featureId, content, worktreePath }
         Delegates to service.sendUserMessage(featureId, content, worktreePath)

      3. StopInteractiveSessionUseCase
         @injectable() with @inject('IInteractiveSessionService')
         Input: { featureId }
         Delegates to service.stopByFeature(featureId)

      4. GetInteractiveChatStateUseCase
         @injectable() with @inject('IInteractiveSessionService')
         Input: { featureId }
         Delegates to service.getChatState(featureId)

      Register all four in DI container with string tokens.
    state: "Todo"
    dependencies:
      - "task-1"
    acceptanceCriteria:
      - "Four use case files created in application/use-cases/interactive/"
      - "Each uses @injectable() and @inject() decorators"
      - "Each has typed input interface and execute() method"
      - "StartInteractiveSessionUseCase accepts agentType and model"
      - "All four registered in DI container"
      - "pnpm build succeeds"
    tdd:
      red:
        - "Test: StartInteractiveSessionUseCase.execute() delegates to service with all params"
        - "Test: SendInteractiveMessageUseCase.execute() delegates to service"
      green:
        - "Implement use cases and register in DI"
      refactor:
        - "Verify naming consistency with existing use cases"
    estimatedEffort: "M"

  - id: "task-5"
    phaseId: "phase-2"
    title: "Update API routes to use resolve<UseCase>() pattern"
    description: >
      Update all interactive API routes to resolve use cases instead of services:

      - POST /api/interactive/sessions → resolve('StartInteractiveSessionUseCase')
        Accept agentType and model in request body
      - POST /api/interactive/chat/[featureId]/messages → resolve('SendInteractiveMessageUseCase')
      - POST /api/interactive/chat/[featureId]/stop → resolve('StopInteractiveSessionUseCase')
      - GET /api/interactive/chat/[featureId]/messages → resolve('GetInteractiveChatStateUseCase')

      For SSE stream routes, they still need the service (for subscribe). Either:
      (a) Create a SubscribeInteractiveStreamUseCase that wraps subscribeByFeature()
      (b) Or keep the service resolution for the stream route specifically (it's a long-lived SSE
          connection, not a simple request-response)

      Follow the existing pattern in other API routes (e.g., /api/tools/route.ts).
    state: "Todo"
    dependencies:
      - "task-4"
    acceptanceCriteria:
      - "All request-response API routes use resolve<UseCase>() pattern"
      - "POST /sessions accepts agentType and model in body"
      - "No API route resolves IInteractiveSessionService directly (except SSE stream)"
      - "Existing API contract preserved (response shapes unchanged)"
      - "pnpm build succeeds"
    tdd: null
    estimatedEffort: "M"

  # ─────────────────────────────────────────────────────────────
  # PHASE 3 — Session Service Refactoring
  # ─────────────────────────────────────────────────────────────

  - id: "task-6"
    phaseId: "phase-3"
    title: "Refactor InteractiveSessionService to use executor pattern"
    description: >
      Major refactoring of InteractiveSessionService:

      1. Constructor: Replace IInteractiveAgentProcessFactory with IAgentExecutorFactory
      2. startSession(): Accept agentType? and model? parameters
         - Resolve agentType from settings if not provided
         - Call factory.createInteractiveExecutor(agentType, config)
         - Call executor.createSession({ cwd, model, systemPrompt })
         - Store InteractiveAgentSessionHandle in SessionState (replace ChildProcess)
      3. sendMessage(): Use handle.send() instead of stdin.write()
         - Iterate handle.stream() for responses instead of readline parsing
         - Map InteractiveAgentEvent to StreamChunk for SSE subscribers
         - InteractiveAgentEvent mapping (executor already did SDK→Event, now Event→StreamChunk):
           delta → { delta: content, done: false }
           tool_use → { delta: '', done: false, log, activity }
           tool_result → { delta: '', done: false, log, activity }
           status → { delta: '', done: false, log }
           done → { delta: '', done: true }
           error → { delta: '', done: true, log: errorMsg }
      4. stopSession(): Use handle.close() instead of process.kill()
      5. SessionState: Replace ChildProcess fields with InteractiveAgentSessionHandle
         - Remove: activeProcess, lineBuffer, onTurnComplete/Error
         - Add: handle (InteractiveAgentSessionHandle)
         - Keep: sessionId, featureId, worktreePath, claudeSessionId, timer, subscribers, model, agentType
      6. Remove ALL manual process management code:
         - executeTurn(), spawnTurnProcess()
         - attachStdoutParser(), handleStdoutLine()
         - attachCloseHandler()
         - JSON.parse per stdout line
         - readline references
         - child_process imports

      Update IInteractiveSessionService interface to accept agentType and model in startSession().
    state: "Todo"
    dependencies:
      - "task-3"
      - "task-5"
    acceptanceCriteria:
      - "Constructor accepts IAgentExecutorFactory instead of IInteractiveAgentProcessFactory"
      - "startSession() accepts agentType? and model? parameters"
      - "Uses executor.createSession() instead of processFactory.spawn()"
      - "sendMessage() uses handle.send() and handle.stream()"
      - "stopSession() uses handle.close()"
      - "No child_process or readline imports in the service"
      - "No manual JSON parsing in the service"
      - "InteractiveAgentEvent → StreamChunk mapping is clean"
      - "Idle timeout and concurrent cap logic preserved"
      - "IInteractiveSessionService interface updated"
      - "pnpm build succeeds"
    tdd:
      red:
        - "Test: startSession resolves executor via factory"
        - "Test: sendMessage calls handle.send() and iterates handle.stream()"
        - "Test: InteractiveAgentEvent mapped to StreamChunk correctly"
        - "Test: stopSession calls handle.close()"
        - "Test: idle timeout still resets on message activity"
      green:
        - "Refactor service to use executor pattern"
        - "Update IInteractiveSessionService interface"
      refactor:
        - "Remove all dead process management methods"
        - "Simplify SessionState interface"
    estimatedEffort: "XL"

  - id: "task-7"
    phaseId: "phase-3"
    title: "Update FeatureContextBuilder for SDK systemPrompt"
    description: >
      Simplify FeatureContextBuilder for SDK systemPrompt.append format.
      Remove any stdin-specific framing (DO NOT EXECUTE headers, JSON wrapping).
      The buildContext() output is passed as systemPrompt string to the executor,
      which passes it as the append field of SDK's preset system prompt.
    state: "Todo"
    dependencies: []
    acceptanceCriteria:
      - "buildContext() returns plain string without stdin-specific framing"
      - "Content still includes feature name, phase, branch, worktree, tasks, PRs"
      - "No 'DO NOT EXECUTE' or stdin-specific headers"
      - "pnpm build succeeds"
    tdd:
      red:
        - "Test: output does not contain stdin-specific framing"
      green:
        - "Remove stdin-specific framing"
      refactor:
        - "Simplify string building"
    estimatedEffort: "S"

  # ─────────────────────────────────────────────────────────────
  # PHASE 4 — Dead Code Cleanup
  # ─────────────────────────────────────────────────────────────

  - id: "task-8"
    phaseId: "phase-4"
    title: "Delete dead code and clean up unused imports"
    description: >
      Delete files no longer needed:
      - packages/core/src/infrastructure/services/interactive/interactive-agent-process.factory.ts
      - packages/core/src/infrastructure/services/interactive/claude-agent-sdk.adapter.ts
      - packages/core/src/application/ports/output/agents/interactive-agent-process-factory.interface.ts
      - packages/core/src/application/ports/output/agents/interactive-agent-adapter.interface.ts
      - tests/unit/infrastructure/services/interactive/interactive-agent-process.factory.test.ts

      Remove DI registrations for deleted types from container.ts.
      Remove re-exports from barrel files.
      Remove all child_process and readline imports from the interactive/ directory.
      Clean up unused imports throughout.
    state: "Todo"
    dependencies:
      - "task-6"
    acceptanceCriteria:
      - "All five files deleted"
      - "DI registrations for deleted types removed"
      - "No remaining imports of deleted files"
      - "No child_process or readline imports in interactive/ directory"
      - "pnpm build succeeds"
      - "pnpm lint passes"
    tdd: null
    estimatedEffort: "S"

  # ─────────────────────────────────────────────────────────────
  # PHASE 5 — Test Updates & Verification
  # ─────────────────────────────────────────────────────────────

  - id: "task-9"
    phaseId: "phase-5"
    title: "Write/update unit tests for new architecture"
    description: >
      Write and update tests for:
      1. ClaudeCodeInteractiveExecutor — mock SDK, test message mapping
      2. AgentExecutorFactory — test createInteractiveExecutor/supportsInteractive
      3. InteractiveSessionService — mock executor, test lifecycle
      4. Use cases — mock service, test delegation
      5. FeatureContextBuilder — verify output format

      Key test patterns:
      - Mock IInteractiveAgentExecutor for service tests (replaces ChildProcess mocks)
      - Mock SDK module for executor tests (vi.mock('@anthropic-ai/claude-agent-sdk'))
      - Use case tests verify delegation with all parameters
    state: "Todo"
    dependencies:
      - "task-6"
    acceptanceCriteria:
      - "Executor message mapping tests cover all SDK message types"
      - "Service tests use executor mocks (no ChildProcess mocks)"
      - "Use case tests verify parameter threading"
      - "All unit tests pass: pnpm test:unit"
    tdd:
      red:
        - "Write failing tests for all new components"
      green:
        - "Verify tests pass against implementation"
      refactor:
        - "Extract shared test fixtures"
    estimatedEffort: "L"

  - id: "task-10"
    phaseId: "phase-5"
    title: "Run full verification suite"
    description: >
      Run all verification commands:
      - pnpm test:unit — all unit tests pass
      - pnpm test:int — integration tests pass (SQLite unchanged)
      - pnpm build — TypeScript compilation succeeds
      - pnpm validate — lint, format, typecheck pass
    state: "Todo"
    dependencies:
      - "task-9"
    acceptanceCriteria:
      - "pnpm test:unit passes with zero failures"
      - "pnpm test:int passes with zero failures"
      - "pnpm build succeeds"
      - "pnpm validate passes"
    tdd: null
    estimatedEffort: "M"

totalEstimate: "M (3-5 days)"

openQuestions: []

content: |
  ## Task Summary

  10 tasks across 5 phases:
  - Phase 1: Executor Pattern Extension (3 tasks — S/M/L)
  - Phase 2: Use Cases & API Routes (2 tasks — M/M)
  - Phase 3: Session Service Refactoring (2 tasks — XL/S)
  - Phase 4: Dead Code Cleanup (1 task — S)
  - Phase 5: Test Updates & Verification (2 tasks — L/M)

  ## Acceptance Checklist

  - [ ] IInteractiveAgentExecutor interface with InteractiveAgentSessionHandle
  - [ ] IAgentExecutorFactory extended with createInteractiveExecutor()
  - [ ] ClaudeCodeInteractiveExecutor using SDK (only SDK import)
  - [ ] 4 use cases: Start, Send, Stop, GetState
  - [ ] API routes use resolve<UseCase>() pattern
  - [ ] agentType + model threaded from UI to executor
  - [ ] Session service uses executor instead of process factory
  - [ ] All dead code deleted (factory, adapter, interfaces)
  - [ ] All tests pass
  - [ ] Full build and validation pass
