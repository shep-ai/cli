# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: hide-branchsync-no-remote
number: 077
branch: feat/077-hide-branchsync-no-remote
oneLiner: >
  Bug: if there is no remote, error is displayed instead of hiding the sync branch section
userQuery: >
  Bug: if there is no remote - error is displayed, instead if there is no remote there is no
  need for sync branch section at all and no need to try and check status
summary: >
  Bug: if there is no remote - error is displayed, instead if there is no remote there is no
  need for sync branch section at all and no need to try and check status
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures:
  []

technologies:
  []

relatedLinks:
  []

# Open questions (must be resolved before implementation)
openQuestions:
  []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Bug: if there is no remote - error is displayed, instead if there is no remote there is no
  need for sync branch section at all and no need to try and check status.

  ## Success Criteria

  - [x] Branch sync section is hidden when repo has no remote
  - [x] No error is displayed when there is no remote
  - [x] Backend use case guards against no-remote

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | Web UI | High | Feature drawer overview tab branch sync section |
  | Use Case | Medium | Backend defense-in-depth guard |

  ## Dependencies

  None identified.

  ## Size Estimate

  **S** - Simple conditional guard

  ---

  _Generated by feature agent — proceed with research_
