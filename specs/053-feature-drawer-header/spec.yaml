# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: feature-drawer-header
number: 053
branch: feat/053-feature-drawer-header
oneLiner: >
  Move the feature id in the feature drawer to be near the delete icon
  and instead of the feature id show the repository name (with icon near it)
  and remove the repository name from the overview
userQuery: >
  Move the feature id in the feature drawer to be near the delete icon
  and instead of the feature id show the repository name (with icon near it)
  and remove the repository name from the overview
summary: >
  Move the feature id in the feature drawer to be near the delete icon
  and instead of the feature id show the repository name (with icon near it)
  and remove the repository name from the overview
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures: []

technologies: []

relatedLinks: []

# Open questions (must be resolved before implementation)
openQuestions: []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Move the feature id in the feature drawer to be near the delete icon
  and instead of the feature id show the repository name (with icon near it)
  and remove the repository name from the overview

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
