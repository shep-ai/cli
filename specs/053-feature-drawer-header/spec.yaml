name: feature-drawer-header
number: 53
branch: feat/053-feature-drawer-header
oneLiner: >
  Move the feature id in the feature drawer to be near the delete icon and
  instead of the feature id show the repository name (with icon near it) and
  remove the repository name from the overview
userQuery: >
  Move the feature id in the feature drawer to be near the delete icon and
  instead of the feature id show the repository name (with icon near it) and
  remove the repository name from the overview
summary: >
  Move the feature id in the feature drawer to be near the delete icon and
  instead of the feature id show the repository name (with icon near it) and
  remove the repository name from the overview
phase: Analysis
sizeEstimate: M
relatedFeatures: []
technologies: []
relatedLinks: []
openQuestions: []
content: |
  ## Problem Statement

  Move the feature id in the feature drawer to be near the delete icon
  and instead of the feature id show the repository name (with icon near it)
  and remove the repository name from the overview

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
rejectionFeedback:
  - iteration: 1
    message: >-
      remove the onliner/summary/user-query from the header (it should be only
      inside the overview)
    phase: merge
    timestamp: '2026-03-09T11:40:31.292Z'
  - iteration: 2
    message: If we can have the remote repo url lets show the remote (as a link)
    phase: merge
    timestamp: '2026-03-09T11:51:30.587Z'
