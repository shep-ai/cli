Full Test Suite Summary — auto-stash-rebase feature evidence
=============================================================

All feature-specific tests run in isolation — 0 failures.

UNIT TESTS (feature-specific, isolated runs)
----------------------------------------------
rebase-feature-on-main.use-case.test.ts:
  Test Files  1 passed (1)
       Tests  20 passed (20)

  Covers: happy path rebase, feature not found, prefix resolution, worktree path,
  repo root path, conflict delegation, conflict resolution failure, SYNC_FAILED
  propagation, non-rebase error propagation, stash before rebase + pop after,
  no-pop when clean, pop after conflict resolution, pop even on failure,
  stash/pop with worktree, resolveStashPop + stashDrop on pop failure,
  distinguishable error when resolveStashPop fails, resolveStashPop after
  rebase conflict resolution, resolveStashPop with worktree path, rebase error
  propagates when both rebase and stash pop fail, resolveStashPop NOT called
  when stashPop succeeds.

conflict-resolution.prompt.test.ts + conflict-resolution.service.test.ts:
  Test Files  2 passed (2)
       Tests  22 passed (22)

  Covers: prompt content (file paths, contents, branch names, markers,
  non-conflicted file warning, attempt/max, feedback, no-feedback first attempt,
  no-git-commands instruction), resolve on first attempt, retry on failure,
  abort after 3 retries, multi-commit (first+second), multi-commit (continue
  succeeds then next conflict), three-commit (first+third only), two-commit
  with retries on second, no-op when no conflicts, cwd passthrough,
  resolveStashPop (happy path, 3 retries, no-op).

git-pr.service.rebase-sync.test.ts (unit):
  Test Files  1 passed (1)
       Tests  41 passed (41)

  Covers: syncMain (fetch on feature branch, pull on main, up-to-date,
  diverged errors, error classification), rebaseOnMain (dirty worktree,
  clean rebase, conflicts, branch not found, checkout + rebase commands),
  getConflictedFiles (parse, empty, backslash normalization, error),
  stageFiles, rebaseContinue (success, new conflict, error), rebaseAbort,
  stash (changes stashed, clean directory, error), stashPop (pop, error),
  stashDrop (drop, error), parseGitError classification.

TOTAL FEATURE UNIT TESTS: 83 passed (0 failed)

INTEGRATION TESTS (feature-specific, isolated run)
----------------------------------------------------
git-pr.service.rebase-sync.test.ts (integration):
  Test Files  1 passed (1)
       Tests  26 passed (26)

  Covers:
  - syncMain: fetch from feature branch, fast-forward on main, up-to-date,
    diverged error, diverged from feature branch, idempotent
  - rebaseOnMain: clean rebase, conflict detection, dirty worktree error,
    branch not found, clean abort, linear history, no-op
  - Helper methods: getConflictedFiles, stageFiles+rebaseContinue, rebaseAbort
  - Stash operations: stash push returns true, clean returns false, message
    format in git stash list, stash pop restores changes, stash drop discards
  - Stash+rebase+pop flow: preserve uncommitted across clean rebase, skip
    stash when clean, preserve with manual conflict resolution, restore after
    failed rebase abort, handle unstaged modifications

TOTAL FEATURE INTEGRATION TESTS: 26 passed (0 failed)

GRAND TOTAL: 109 feature-specific tests passed, 0 failed.
