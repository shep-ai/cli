# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: scheduled-workflows
number: 073
branch: feat/073-scheduled-workflows
oneLiner: "User-defined workflows that run on demand or on a cron schedule via the shep daemon"
summary: >
  Add a workflow engine to shep that lets users define reusable automation scripts
  (expressed as agent prompts with optional tool constraints) and execute them on
  demand via CLI/web UI or on a recurring cron schedule managed by the daemon
  process. Workflows are persisted in SQLite, scheduled via a new daemon-hosted
  scheduler service, and executed as agent tasks. The two motivating examples are
  auto-closing resolved GitHub issues with evidence and rebasing all tracked
  feature branches on main.
phase: Requirements
sizeEstimate: L

relatedFeatures: []

technologies:
  - "TypeSpec (domain model definitions)"
  - "better-sqlite3 (workflow and schedule persistence)"
  - "tsyringe (dependency injection)"
  - "croner (cron expression parsing and scheduling)"
  - "LangGraph (agent graph orchestration)"
  - "Commander.js (CLI commands)"
  - "Next.js (web UI for workflow management)"
  - "shadcn/ui (workflow UI components)"
  - "Vitest (testing)"
  - "umzug (database migrations)"

relatedLinks:
  - title: "node-cron - npm"
    url: "https://www.npmjs.com/package/node-cron"
  - title: "croner - lightweight cron scheduler"
    url: "https://www.npmjs.com/package/croner"
  - title: "GitHub Actions workflow syntax reference"
    url: "https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions"

openQuestions:
  - question: "Which cron library should be used for schedule evaluation?"
    resolved: true
    options:
      - option: "croner"
        description: "Lightweight, zero-dependency cron scheduler with excellent TypeScript support, sub-second precision, timezone awareness, and active maintenance. Supports both parsing and triggering. ~15 KB bundle size."
        selected: true
      - option: "node-cron"
        description: "Popular cron scheduler with large community adoption. Heavier dependency tree, weaker TypeScript types, no timezone support without additional libraries. ~50 KB bundle size."
        selected: false
      - option: "cron (npm)"
        description: "Full-featured cron library with timezone support via luxon. Heaviest option with multiple transitive dependencies. Mature but more complexity than needed."
        selected: false
    selectionRationale: "croner is recommended because it has zero dependencies (aligning with shep's minimal-dependency philosophy), native TypeScript definitions, built-in timezone support, and is significantly lighter than alternatives. It supports both cron expression validation and scheduling triggers in a single package."
    answer: "croner"

  - question: "How should workflow definitions be stored and managed?"
    resolved: true
    options:
      - option: "SQLite-only storage"
        description: "Workflow definitions stored entirely in the shep SQLite database. Users create/edit workflows via CLI commands or web UI. Simple, centralized, consistent with existing shep patterns for features and settings."
        selected: true
      - option: "YAML file-based definitions"
        description: "Workflows defined as YAML files in a .shep/workflows/ directory, similar to GitHub Actions. Git-trackable and shareable. Requires file watching and sync logic."
        selected: false
      - option: "Hybrid (YAML + SQLite)"
        description: "Define workflows in YAML files but sync to SQLite for runtime scheduling. Offers git-trackability plus database query performance. Adds complexity of two-source synchronization."
        selected: false
    selectionRationale: "SQLite-only storage is recommended for the initial implementation because it is consistent with how shep stores all other entities (features, settings, sessions), avoids the complexity of file watching and two-source sync, and keeps the implementation scope manageable. A future enhancement could add YAML import/export for workflow portability."
    answer: "SQLite-only storage"

  - question: "What level of workflow step complexity should the initial release support?"
    resolved: true
    options:
      - option: "Single-step agent prompt"
        description: "Each workflow is a single agent prompt with optional tool constraints. The agent receives the prompt and executes it end-to-end. Simple to implement, covers the two motivating use cases (issue triage, branch rebase), and avoids the complexity of multi-step orchestration."
        selected: true
      - option: "Multi-step sequential pipeline"
        description: "Workflows consist of ordered steps, each with its own prompt and tool constraints. Steps execute sequentially with output passed between them. More powerful but significantly more complex domain model and execution engine."
        selected: false
      - option: "DAG-based workflow graph"
        description: "Full directed acyclic graph of steps with parallel execution, conditional branches, and data flow between nodes. Most powerful but extremely complex, essentially building a workflow engine like Temporal or Airflow."
        selected: false
    selectionRationale: "Single-step agent prompt is recommended because both motivating use cases (issue triage and branch rebase) can be expressed as a single comprehensive prompt. The agent's LangGraph-based reasoning is already capable of multi-step execution within a single prompt. Starting simple avoids premature abstraction and keeps the feature deliverable within the L size estimate. Multi-step pipelines can be added later as an enhancement."
    answer: "Single-step agent prompt"

  - question: "How should concurrent workflow execution be handled?"
    resolved: true
    options:
      - option: "Sequential execution with queue"
        description: "Only one workflow runs at a time. Subsequent triggers are queued and executed in order. Prevents resource contention and simplifies error handling. May delay time-sensitive workflows."
        selected: true
      - option: "Parallel execution with concurrency limit"
        description: "Multiple workflows can run simultaneously up to a configurable limit (e.g., 3). Better throughput but requires resource management, conflict detection for workflows touching the same repo, and more complex status tracking."
        selected: false
      - option: "Unrestricted parallel execution"
        description: "All triggered workflows run immediately with no limits. Maximum throughput but risks resource exhaustion and git conflicts when multiple workflows modify the same repository."
        selected: false
    selectionRationale: "Sequential execution with queue is recommended because workflows operate on a shared git repository where concurrent modifications would cause conflicts. The existing agent runner uses a similar single-execution model. Sequential execution is simpler to implement, reason about, and debug. A concurrency limit can be added later if needed."
    answer: "Sequential execution with queue"

  - question: "Should workflow execution history have a retention policy?"
    resolved: true
    options:
      - option: "Time-based retention (30 days)"
        description: "Automatically purge execution records older than 30 days. Prevents unbounded database growth. Simple to implement via a periodic cleanup query."
        selected: true
      - option: "Count-based retention (last 100 per workflow)"
        description: "Keep only the most recent 100 executions per workflow. Predictable storage per workflow but could lose recent history for high-frequency workflows."
        selected: false
      - option: "No automatic retention"
        description: "Keep all execution history indefinitely. Simplest implementation but risks unbounded database growth over months of operation."
        selected: false
    selectionRationale: "Time-based retention of 30 days is recommended as a sensible default that prevents unbounded growth while preserving enough history for debugging and auditing. The retention period should be configurable via settings. A cleanup routine can run as part of the scheduler service's periodic tick."
    answer: "Time-based retention (30 days)"

  - question: "What scope should the web UI have in the initial release?"
    resolved: true
    options:
      - option: "List and status view only"
        description: "Web UI shows a read-only list of workflows with their schedules, status, and recent execution history. Creating and editing workflows is CLI-only. Reduces frontend scope significantly."
        selected: false
      - option: "Full CRUD with execution view"
        description: "Web UI supports creating, editing, deleting, enabling/disabling workflows, viewing execution history and logs, and manually triggering runs. Complete management experience."
        selected: false
      - option: "Management view with manual trigger"
        description: "Web UI shows workflow list, schedule status, execution history, and allows enabling/disabling and manual triggering. Workflow creation and editing remain CLI-only to keep the web scope focused."
        selected: true
    selectionRationale: "Management view with manual trigger is recommended as a pragmatic middle ground. Users will create workflows via CLI (which is natural for defining prompts and tool constraints), but the web UI provides monitoring, history, and quick actions (enable/disable/trigger). This reduces frontend complexity while still providing real value over CLI-only interaction."
    answer: "Management view with manual trigger"

  - question: "How should workflow tool constraints be specified?"
    resolved: true
    options:
      - option: "Allowlist of tool names"
        description: "Users specify which tools the workflow agent is allowed to use (e.g., git, github, file-read). Explicit and secure — the agent cannot access tools not in the list. Requires users to know available tool names."
        selected: true
      - option: "Denylist of tool names"
        description: "Users specify which tools to block. Agent can use everything else. Easier to configure but less secure — new tools are automatically available unless explicitly blocked."
        selected: false
      - option: "No tool constraints"
        description: "The workflow agent has access to all tools available to the configured agent type. Simplest but offers no sandboxing. Relies entirely on prompt engineering for safety."
        selected: false
    selectionRationale: "Allowlist of tool names is recommended because it follows the principle of least privilege. Workflows running unattended on a schedule should have explicit boundaries on what they can do. An allowlist ensures that adding new tools to shep doesn't inadvertently expand the capabilities of existing workflows. A sensible default set can be pre-configured per workflow template."
    answer: "Allowlist of tool names"

  - question: "Should the feature include built-in workflow templates?"
    resolved: true
    options:
      - option: "Two starter templates"
        description: "Ship with two templates matching the motivating use cases: (1) GitHub issue triage and (2) branch rebase. Users can create workflows from these templates or from scratch. Provides immediate value and serves as documentation-by-example."
        selected: true
      - option: "No templates"
        description: "Users create all workflows from scratch. Keeps the implementation minimal but requires users to figure out effective prompts and tool configurations themselves."
        selected: false
      - option: "Template marketplace"
        description: "A registry of community-contributed templates that users can browse and install. Powerful but massive scope increase far beyond the core feature."
        selected: false
    selectionRationale: "Two starter templates are recommended because they directly address the motivating use cases, validate the workflow engine end-to-end during development, and serve as practical examples for users creating their own workflows. The templates also provide excellent test fixtures."
    answer: "Two starter templates"

content: |
  ## Problem Statement

  Shep currently orchestrates AI-driven feature development through a linear SDLC
  pipeline (analyze -> requirements -> research -> plan -> implement -> review). However,
  users also need to run recurring maintenance and automation tasks that don't fit the
  feature lifecycle — for example:

  1. **GitHub issue triage**: Scan open issues, check if they're resolved by existing
     commits or PRs, add evidence comments, and close them automatically.
  2. **Branch maintenance**: Rebase all tracked shep feature branches on main and
     resolve merge conflicts.
  3. **Custom automations**: Any user-defined workflow that combines agent intelligence
     with repository operations on a schedule.

  Today, users must manually trigger these operations or build external automation.
  There is no way to define reusable workflows within shep or schedule them to run
  periodically via the daemon.

  ## Success Criteria

  - [ ] Users can create a workflow definition via `shep workflow create` with a name, agent prompt, and optional tool constraints
  - [ ] Users can list all workflows via `shep workflow list` showing name, schedule status, last run time, and enabled state
  - [ ] Users can manually trigger a workflow via `shep workflow run <name>` and see execution output
  - [ ] Users can attach a cron schedule to a workflow via `shep workflow schedule <name> --cron "<expression>"`
  - [ ] The daemon automatically executes scheduled workflows at the correct times when running
  - [ ] Users can enable/disable a workflow's schedule via `shep workflow enable/disable <name>`
  - [ ] Users can view execution history via `shep workflow history <name>` showing status, duration, and timestamps
  - [ ] Users can delete a workflow via `shep workflow delete <name>`
  - [ ] The web UI displays a workflow management page with list view, execution history, and enable/disable/trigger controls
  - [ ] Workflow execution emits events through the notification bus (started, completed, failed)
  - [ ] Execution history older than 30 days is automatically purged
  - [ ] Two built-in templates are available: GitHub issue triage and branch rebase
  - [ ] All domain models are defined in TypeSpec and compiled to TypeScript
  - [ ] All new code has unit test coverage with TDD methodology
  - [ ] Web UI components have colocated Storybook stories

  ## Functional Requirements

  - **FR-1**: The system MUST allow users to create a workflow definition consisting of a unique name, human-readable description, an agent prompt (the instruction the AI agent will execute), and an optional allowlist of permitted tools.
  - **FR-2**: The system MUST allow users to list all workflow definitions with their current state (enabled/disabled, last execution time, next scheduled run, cron expression if scheduled).
  - **FR-3**: The system MUST allow users to manually trigger execution of any workflow on demand via CLI (`shep workflow run <name>`) or web UI trigger button, regardless of whether it has a cron schedule.
  - **FR-4**: The system MUST allow users to attach, update, or remove a cron schedule from a workflow. Cron expressions follow standard 5-field format (minute, hour, day-of-month, month, day-of-week) with optional timezone specification.
  - **FR-5**: The daemon process MUST evaluate cron schedules and automatically trigger workflow execution at the correct times. The scheduler checks for due workflows at a regular polling interval (default: 60 seconds).
  - **FR-6**: The system MUST allow users to enable or disable a workflow's schedule without deleting the cron expression. Disabled workflows retain their schedule but do not execute automatically. Manual triggering is still allowed on disabled workflows.
  - **FR-7**: The system MUST persist a complete execution history for each workflow run, including: execution ID, workflow ID, trigger type (manual or scheduled), start time, end time, duration, status (queued, running, completed, failed, cancelled), and output/error summary.
  - **FR-8**: The system MUST execute workflows sequentially — only one workflow runs at a time. Additional triggers (manual or scheduled) are queued in FIFO order and executed when the current run completes.
  - **FR-9**: The system MUST allow users to delete a workflow definition. Deleting a workflow cancels any pending scheduled runs and removes the schedule, but preserves execution history for audit purposes (subject to retention policy).
  - **FR-10**: The system MUST emit typed events through the existing NotificationEventBus when a workflow execution starts, completes, or fails, enabling notification delivery through existing channels.
  - **FR-11**: The system MUST automatically purge execution history records older than a configurable retention period (default: 30 days). Purging runs as part of the scheduler service's periodic tick.
  - **FR-12**: The system MUST provide two built-in workflow templates: (1) GitHub issue triage — scans open issues, identifies resolved ones, adds evidence comments, and closes them; (2) Branch rebase — rebases all tracked shep feature branches on main.
  - **FR-13**: The web UI MUST display a workflow management page showing a list of all workflows with their schedule, status, and recent execution history. The UI MUST support enabling/disabling workflows and manually triggering runs. Workflow creation and editing remain CLI-only.
  - **FR-14**: The system MUST allow users to view detailed execution history for a specific workflow via CLI (`shep workflow history <name>`) and web UI, including status, duration, trigger type, and output/error summary.
  - **FR-15**: The system MUST validate cron expressions at schedule creation time and reject invalid expressions with a clear error message. The validation MUST use the same cron library (croner) that evaluates schedules at runtime to ensure consistency.
  - **FR-16**: The system MUST allow users to update an existing workflow's name, description, prompt, or tool constraints via `shep workflow update <name>`. Updating a workflow does not affect currently running or queued executions.
  - **FR-17**: The system MUST gracefully handle daemon restarts — on startup, the scheduler service MUST load all enabled scheduled workflows from the database and resume scheduling. Missed scheduled runs during downtime are NOT retroactively executed; the next run occurs at the next matching cron time.

  ## Non-Functional Requirements

  - **NFR-1 (Performance)**: The scheduler polling loop MUST complete in under 100ms per tick, including database queries for due workflows and queue management. The scheduler MUST NOT block the daemon's event loop or degrade web server responsiveness.
  - **NFR-2 (Reliability)**: Workflow execution MUST be crash-safe. If the daemon crashes during a workflow run, the execution record MUST be marked as failed (not stuck in "running") on next startup via a recovery sweep.
  - **NFR-3 (Storage)**: Workflow execution history MUST NOT grow unbounded. The automatic 30-day retention policy MUST be enforced. With the default purge policy, the database should not accumulate more than ~50,000 execution records under normal usage patterns (10 workflows, 4 runs/day each).
  - **NFR-4 (Security)**: Workflow tool constraints MUST be enforced at the agent execution level — the agent MUST NOT be able to bypass the allowlist. Prompts stored in the database MUST be treated as user input and not interpreted as system commands.
  - **NFR-5 (UX - CLI)**: All `shep workflow` commands MUST provide clear, human-readable output. List output MUST be formatted as a table. Error messages MUST include actionable remediation steps (e.g., "Invalid cron expression. Example: '0 9 * * MON' runs every Monday at 9am").
  - **NFR-6 (UX - Web)**: The workflow management page MUST load in under 2 seconds. Execution status MUST update in near-real-time (within 5 seconds) via polling or server-sent events.
  - **NFR-7 (Maintainability)**: All domain models MUST be defined in TypeSpec first. All new services MUST use constructor injection via tsyringe. All repositories MUST implement output port interfaces defined in the application layer. The feature MUST follow the existing Clean Architecture layer boundaries.
  - **NFR-8 (Testability)**: The scheduler service MUST be testable without real timers — the cron evaluation logic MUST accept an injectable clock/time provider. All repository methods MUST be testable with an in-memory or test SQLite database.
  - **NFR-9 (Observability)**: Workflow execution MUST produce structured log output including workflow name, execution ID, trigger type, and duration. Failed executions MUST log the error with full context.
  - **NFR-10 (Graceful Degradation)**: If the cron library fails to parse a schedule, the workflow MUST remain in a disabled state with a clear error stored. A single failing workflow MUST NOT prevent other workflows from executing.

  ## Product Questions & AI Recommendations

  | # | Question | AI Recommendation | Rationale |
  | - | -------- | ----------------- | --------- |
  | 1 | Which cron library? | croner | Zero dependencies, native TypeScript, built-in timezone support, lightweight |
  | 2 | How to store workflow definitions? | SQLite-only | Consistent with existing patterns, avoids file-sync complexity |
  | 3 | Workflow step complexity? | Single-step agent prompt | Covers motivating use cases, avoids premature abstraction |
  | 4 | Concurrent execution model? | Sequential with queue | Avoids git conflicts, matches existing agent runner model |
  | 5 | Execution history retention? | 30-day time-based | Prevents unbounded growth, configurable default |
  | 6 | Web UI scope? | Management view with trigger | CLI for creation, web for monitoring and quick actions |
  | 7 | Tool constraint model? | Allowlist | Principle of least privilege for unattended execution |
  | 8 | Built-in templates? | Two starter templates | Validates engine, provides examples, immediate user value |

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | `tsp/domain/entities/` | High | New Workflow and WorkflowExecution TypeSpec models defining the domain |
  | `packages/core/src/domain/generated/output.ts` | High | Auto-generated from TypeSpec — new types will appear here after compile |
  | `tsp/common/enums/` | Medium | New enums for WorkflowTriggerType (manual, cron), WorkflowExecutionStatus (queued, running, completed, failed, cancelled) |
  | `packages/core/src/application/use-cases/` | High | New use cases: CreateWorkflow, UpdateWorkflow, DeleteWorkflow, ListWorkflows, RunWorkflow, ScheduleWorkflow, GetWorkflowHistory, EnableWorkflow, DisableWorkflow |
  | `packages/core/src/application/ports/output/repositories/` | High | New IWorkflowRepository and IWorkflowExecutionRepository interfaces |
  | `packages/core/src/application/ports/output/services/` | Medium | New ISchedulerService interface for cron-based scheduling |
  | `packages/core/src/infrastructure/repositories/` | High | New SQLiteWorkflowRepository and SQLiteWorkflowExecutionRepository |
  | `packages/core/src/infrastructure/persistence/sqlite/migrations/` | High | New migration(s) for workflow tables (workflows, workflow_executions) |
  | `packages/core/src/infrastructure/persistence/sqlite/mappers/` | Medium | New mappers for workflow domain entities |
  | `packages/core/src/infrastructure/services/` | High | New WorkflowSchedulerService managing cron jobs and execution queue via the daemon |
  | `packages/core/src/infrastructure/di/container.ts` | Medium | Register new repositories, services, and use cases |
  | `src/presentation/cli/commands/` | High | New `workflow` command group (create, update, list, run, schedule, enable, disable, history, delete) |
  | `src/presentation/web/` | Medium | Workflow management UI page with list view, execution history, and action controls |
  | `packages/core/src/infrastructure/services/daemon/` | Medium | Daemon startup needs to initialize and start the scheduler service |
  | `src/presentation/cli/commands/_serve.command.ts` | Low | Initialize scheduler service alongside existing watchers |
  | `packages/core/src/infrastructure/services/notifications/` | Low | Workflow events emitted through existing notification bus |

  ## Dependencies

  **Existing Code Dependencies:**
  - DI container (`infrastructure/di/container.ts`) — registration of new services
  - Agent execution infrastructure (`infrastructure/services/agents/`) — running workflow agent tasks
  - Notification system (`infrastructure/services/notifications/`) — emitting workflow events
  - Daemon lifecycle (`infrastructure/services/daemon/`) — hosting the scheduler process
  - Git service (`infrastructure/services/git/`) — branch operations for rebase workflows
  - GitHub integration (`infrastructure/services/external/`) — issue/PR operations for triage workflows
  - SQLite persistence layer — workflow data storage
  - TypeSpec compilation pipeline — domain model generation
  - Watcher service pattern (`NotificationWatcherService`, `PrSyncWatcherService`) — architecture template for scheduler service

  **New Library Dependencies:**
  - `croner` — cron expression parsing, validation, and next-run calculation. Zero-dependency,
    TypeScript-native. Used for schedule evaluation in the WorkflowSchedulerService and
    cron expression validation in CLI/use case input handling.

  ## Size Estimate

  **L (week+)** — This feature spans all four architecture layers (domain, application,
  infrastructure, presentation) and introduces new domain entities (Workflow, WorkflowExecution),
  9+ use cases, a new scheduler service with execution queue, a new CLI command group with
  9 subcommands, new database migrations, and a web UI management page. The agent execution
  integration, built-in templates, and crash recovery logic add additional complexity. The
  single-step workflow model and CLI-only creation scope keep the feature within L rather
  than growing to XL.

  ---

  _Requirements defined — proceed with research phase_
