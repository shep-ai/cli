# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: docusaurus-docs-site
number: 001
branch: feat/001-docusaurus-docs-site
oneLiner: Add Docusaurus v3 documentation site with automated GitHub Pages CI/CD deployment. Set up a /docs directory using 'npx create-docusaurus@latest' with the classic theme. Configure docusaurus.config.js with the correct GitHub org (shep-ai), repo name (shep), and baseUrl for GitHub Pages. Add a GitHub Actions workflow at .github/workflows/docs.yml that triggers on push to main, builds the Docusaurus site, and deploys to the gh-pages branch using peaceiris/actions-gh-pages. Include an initial docs structure with at minimum: Getting Started, CLI Reference (covering all shep commands: feat, repo, agent, run, session, ide, tools, install, ui, start, stop, restart, status, settings, version, upgrade), and a Concepts section explaining the Shep SDLC platform. The docs should be written in markdown, match the existing branding in README.md, and be ready for production use on GitHub Pages.
userQuery: >
  Add Docusaurus v3 documentation site with automated GitHub Pages CI/CD deployment. Set up a /docs directory using 'npx create-docusaurus@latest' with the classic theme. Configure docusaurus.config.js with the correct GitHub org (shep-ai), repo name (shep), and baseUrl for GitHub Pages. Add a GitHub Actions workflow at .github/workflows/docs.yml that triggers on push to main, builds the Docusaurus site, and deploys to the gh-pages branch using peaceiris/actions-gh-pages. Include an initial docs structure with at minimum: Getting Started, CLI Reference (covering all shep commands: feat, repo, agent, run, session, ide, tools, install, ui, start, stop, restart, status, settings, version, upgrade), and a Concepts section explaining the Shep SDLC platform. The docs should be written in markdown, match the existing branding in README.md, and be ready for production use on GitHub Pages.
summary: >
  Add Docusaurus v3 documentation site with automated GitHub Pages CI/CD deployment. Set up a /docs directory using 'npx create-docusaurus@latest' with the classic theme. Configure docusaurus.config.js with the correct GitHub org (shep-ai), repo name (shep), and baseUrl for GitHub Pages. Add a GitHub Actions workflow at .github/workflows/docs.yml that triggers on push to main, builds the Docusaurus site, and deploys to the gh-pages branch using peaceiris/actions-gh-pages. Include an initial docs structure with at minimum: Getting Started, CLI Reference (covering all shep commands: feat, repo, agent, run, session, ide, tools, install, ui, start, stop, restart, status, settings, version, upgrade), and a Concepts section explaining the Shep SDLC platform. The docs should be written in markdown, match the existing branding in README.md, and be ready for production use on GitHub Pages.
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures:
  []

technologies:
  []

relatedLinks:
  []

# Open questions (must be resolved before implementation)
openQuestions:
  []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Add Docusaurus v3 documentation site with automated GitHub Pages CI/CD deployment. Set up a /docs directory using 'npx create-docusaurus@latest' with the classic theme. Configure docusaurus.config.js with the correct GitHub org (shep-ai), repo name (shep), and baseUrl for GitHub Pages. Add a GitHub Actions workflow at .github/workflows/docs.yml that triggers on push to main, builds the Docusaurus site, and deploys to the gh-pages branch using peaceiris/actions-gh-pages. Include an initial docs structure with at minimum: Getting Started, CLI Reference (covering all shep commands: feat, repo, agent, run, session, ide, tools, install, ui, start, stop, restart, status, settings, version, upgrade), and a Concepts section explaining the Shep SDLC platform. The docs should be written in markdown, match the existing branding in README.md, and be ready for production use on GitHub Pages.

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
