CreateFeatureUseCase Tests — 57/57 passing (includes 11 skill injection tests)
================================================================
vitest v4.0.18 — project: node
Run: npx vitest run --project node tests/unit/application/use-cases/features/create-feature.use-case.test.ts --reporter=verbose

CreateFeatureUseCase > without parentId (baseline)
  ✓ should create a feature and spawn the agent
  ✓ should persist the feature with no parentId
  ✓ should accept parentId in the input without TypeScript errors

CreateFeatureUseCase > with parentId — parent validation
  ✓ should throw when parentId references a non-existent feature

CreateFeatureUseCase > with parentId — gate logic (child lifecycle)
  ✓ should create child in Blocked state when parent lifecycle is Blocked
  ✓ should create child in Blocked state when parent lifecycle is Started
  ✓ should create child in Blocked state when parent lifecycle is Planning (< Implementation)
  ✓ should create child in Started state when parent lifecycle is Implementation
  ✓ should create child in Started state when parent lifecycle is Review
  ✓ should create child in Started state when parent lifecycle is Maintain

CreateFeatureUseCase > with parentId — persistence
  ✓ should persist parentId on the created feature

CreateFeatureUseCase > with parentId — repositoryPath resolution
  ✓ should use parent repositoryPath instead of input repositoryPath
  ✓ should use input repositoryPath when no parentId is given

CreateFeatureUseCase > cycle detection
  ✓ should reject a self-reference (A → A)
  ✓ should reject a two-node cycle (A.parentId → B, B.parentId → A)
  ✓ should reject a deep cycle (A → B → C → D → A)
  ✓ should allow a valid deep chain without cycles (A → B → C)

CreateFeatureUseCase > repository creation guard
  ✓ throws if repositoryRepo.create() returns falsy

CreateFeatureUseCase > fast mode
  ✓ should set lifecycle to Implementation when fast=true
  ✓ should set lifecycle to Requirements when fast=false
  ✓ should set lifecycle to Requirements when fast is undefined
  ✓ should pass fast flag to specInitializer.initialize() as mode
  ✓ should not pass mode to specInitializer.initialize() when fast is false
  ✓ should pass fast=true to agentProcess.spawn() options
  ✓ should not include fast in spawn options when fast is false
  ✓ should still set lifecycle to Blocked for child feature with fast=true when parent is Blocked

CreateFeatureUseCase > pending flag
  ✓ should accept pending field in CreateFeatureInput without TypeScript errors
  ✓ should create feature in Pending lifecycle when pending=true
  ✓ should not spawn agent when pending=true
  ✓ should create feature in Pending lifecycle when pending=true and parentId is set
  ✓ should create feature in Pending lifecycle when pending=true, parentId set, and parent in POST_IMPLEMENTATION
  ✓ should preserve existing behavior when pending is false
  ✓ should preserve existing behavior when pending is undefined
  ✓ should set fast feature to Pending lifecycle when pending=true and fast=true

CreateFeatureUseCase > model field wiring
  ✓ should pass model to spawn when input.model is provided
  ✓ should not set model on spawn options when input.model is absent
  ✓ should accept model field in CreateFeatureInput without TypeScript errors

CreateFeatureUseCase > rebaseBeforeBranch
  ✓ should sync main before creating worktree by default
  ✓ should sync main when rebaseBeforeBranch is explicitly true
  ✓ should skip sync when rebaseBeforeBranch is false
  ✓ should still create worktree when syncMain fails
  ✓ should accept rebaseBeforeBranch field in CreateFeatureInput

CreateFeatureUseCase > agent validation before spawn
  ✓ should throw with meaningful error when agent is not available
  ✓ should mark agent run as failed when agent is not available
  ✓ should spawn agent when validation passes
  ✓ should validate the input agentType when provided

CreateFeatureUseCase > skill injection
  ✓ should call skillInjector.inject() when injectSkills is true
  ✓ should NOT call inject() when injectSkills is false
  ✓ should NOT call inject() when skills list is empty
  ✓ should NOT call inject() when skillInjection config is undefined
  ✓ should use settings.workflow.skillInjection.enabled when injectSkills is undefined
  ✓ should NOT inject when settings.enabled is false and injectSkills is undefined
  ✓ should override settings.enabled=false when injectSkills=true
  ✓ should override settings.enabled=true when injectSkills=false
  ✓ should continue with agent spawn when inject() throws
  ✓ should still complete feature creation when inject() throws
  ✓ should default injectSkills to false when both input and settings are undefined

Test Files  1 passed (1)
     Tests  57 passed (57)
  Start at  17:44:25
  Duration  180ms
