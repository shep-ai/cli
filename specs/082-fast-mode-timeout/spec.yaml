name: "fast-mode-timeout"
number: 82
branch: "feat/082-fast-mode-timeout"
oneLiner: "Fast mode has a default timeout of 30 minutes it should be configurable like the rest stages"
userQuery: "Fast mode has a default timeout of 30 minutes it should be configurable like the rest stages\n"
summary: "Fast mode has a default timeout of 30 minutes it should be configurable like the rest stages\n"
phase: "merge"
sizeEstimate: "M"
relatedFeatures: []
technologies: []
relatedLinks: []
openQuestions: []
content: |
  ## Problem Statement

  Fast mode has a default timeout of 30 minutes it should be configurable like the rest stages

  ## Success Criteria

  - [x] fastImplementMs field added to StageTimeouts TypeSpec model
  - [x] Database migration adds stage_timeout_fast_implement_ms column
  - [x] Settings mapper bidirectionally maps fastImplementMs <-> stage_timeout_fast_implement_ms
  - [x] Settings page UI displays and saves the fast-implement timeout
  - [x] Node-helpers resolves per-stage timeout for fast-implement from settings
  - [x] JSON schema updated with fastImplementMs field
  - [x] All 8 i18n translation files updated
  - [x] All unit tests pass (5292/5292)

  ## Evidence — Settings Page Connected to Mapper (NOT Hardcoded)

  The settings page receives `Settings` as props from the `loadSettings()` server action,
  which calls `LoadSettingsUseCase` → `fromDatabase()` mapper. It does NOT use hardcoded values.

  ### Mapper Evidence (settings.mapper.ts)

  - Line 92: `stage_timeout_fast_implement_ms: number | null` in SettingsRow interface
  - Line 207: `toDatabase()` maps `fastImplementMs → stage_timeout_fast_implement_ms`
  - Lines 272-273: `fromDatabase()` maps `stage_timeout_fast_implement_ms → fastImplementMs`

  ### Settings Page Evidence (settings-page-client.tsx)

  - Props: `settings: Settings` (from loadSettings server action, NOT hardcoded)
  - Line 453: State initialized from `stageTimeoutsConfig?.fastImplementMs` (from props)
  - Line 609: Save payload includes `fastImplementMs: secondsToMs(fastImplementTimeout)`
  - Lines 1300-1316: UI renders TimeoutSlider bound to fastImplementTimeout state

  ### Workflow Settings Section Evidence (workflow-settings-section.tsx)

  - Props: `workflow: WorkflowConfig` (from parent, NOT hardcoded)
  - Line 59: State from `st?.fastImplementMs` (where st = workflow.stageTimeouts from props)
  - Line 139: Save payload includes `fastImplementMs`
  - Lines 257-263: Field config for 'Fast Implement' with state/setter/original tracking

  ### Node-Helpers Evidence (node-helpers.ts)

  - Reads `settings.workflow.stageTimeouts.fastImplementMs` for fast-implement nodes
  - Falls back to 1,800,000ms default when not configured
  - Tested: custom value (600,000ms) and fallback both verified

  ### Test Evidence (see evidence/ directory)

  - settings-mapper-test-results.txt: 67/67 passed (includes fastImplementMs round-trip)
  - node-helpers-test-results.txt: 47/47 passed (includes fast-implement timeout tests)
  - workflow-settings-section-test-results.txt: 15/15 passed (includes fastImplementTimeout input)
  - full-test-suite-results.txt: 5292/5292 passed (zero regressions)
  - data-flow-evidence.txt: Complete line-by-line data flow documentation

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TypeSpec domain model | Added fastImplementMs to StageTimeouts | Source of truth for the field |
  | Database migration | New column stage_timeout_fast_implement_ms | Persistent storage |
  | Settings mapper | Bidirectional mapping | DB <-> domain conversion |
  | Settings page UI | New timeout slider | User-facing configuration |
  | Node-helpers | Per-stage timeout resolution | Runtime behavior change |
  | JSON schema | Added fastImplementMs | External config validation |
  | i18n translations | 8 language files | Localized labels |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** — Medium: touches 7 layers but each change is small and follows existing patterns.

  ---

  _Generated by feature agent — merge review with evidence_
rejectionFeedback:
  - iteration: 1
    message: "where is the evidence that the settings is available in the settings page?\nit seems that the settings page settings are hard coded and not related to the settings mapper"
    phase: "merge"
    timestamp: "2026-04-02T18:17:38.531Z"
  - iteration: 2
    message: "update the merge review to have evidence"
    phase: "merge"
    timestamp: "2026-04-02T21:55:14.627Z"
