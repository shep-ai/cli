# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: shep-cicd-pipeline
number: 062
branch: feat/062-shep-cicd-pipeline
oneLiner: >
  Add a manually-triggered CI/CD workflow that runs shep end-to-end on a real
  repository (shep-ai/sheep), creating a feature, pushing, opening a PR, and
  merging it autonomously.
userQuery: >
  I want to add CICD which runs one feature using shep with claude on real
  repository. We have CLAUDE_CODE_OAUTH_TOKEN secret with claude token. I want
  single cicd wf that clones https://github.com/shep-ai/sheep repo, we have
  RELEASE_TOKEN with git secret with admin access to that repo. After cloning
  repo i want to run "shep feat new" with some simple fast feature (in FAST
  MODE), with allow-all and push+pr. We want to test that PR is created and
  merged on that repo and feature is marked as completed in shep. For now, i
  want this wf to be MANUALLY triggered.
summary: >
  Add a manually-triggered CI/CD workflow that runs shep end-to-end on a real
  repository (shep-ai/sheep), creating a feature, pushing, opening a PR, and
  merging it autonomously.
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures: []

technologies: []

relatedLinks: []

# Open questions (must be resolved before implementation)
openQuestions: []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Add a manually-triggered GitHub Actions workflow that validates shep's
  end-to-end feature lifecycle on a real repository (shep-ai/sheep).

  ## Success Criteria

  - [ ] Workflow runs shep feat new with a randomized prompt
  - [ ] Feature is created, implemented, pushed, PR opened, and merged by shep
  - [ ] Workflow polls shep feat show until completion or failure
  - [ ] CI passes on the shep-ai/cli PR

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | CI | High | New workflow file |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - Single workflow file with polling logic

  ---

  _Generated by feature agent — proceed with research_
