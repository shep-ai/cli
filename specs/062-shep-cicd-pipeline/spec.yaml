# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: shep-cicd-pipeline
number: 062
branch: feat/062-shep-cicd-pipeline
oneLiner: I want to add CICD which runs one feature using shep with claude on real repository.
We have CLAUDE_CODE_OAUTH_TOKEN secret with claude token.
I want single cicd wf that clones https://github.com/shep-ai/sheep repo, we have RELEASE_TOKEN with git secret with admin access to that repo.
After cloning repo i want to run "shep feat new" with some simple fast feature (in FAST MODE), with allow-all and push+pr.

We want to test that PR is created and merged on that repo and feature is marked as completed in shep. 

For now, i want this wf to be MANUALLY triggered
userQuery: >
  I want to add CICD which runs one feature using shep with claude on real repository.
We have CLAUDE_CODE_OAUTH_TOKEN secret with claude token.
I want single cicd wf that clones https://github.com/shep-ai/sheep repo, we have RELEASE_TOKEN with git secret with admin access to that repo.
After cloning repo i want to run "shep feat new" with some simple fast feature (in FAST MODE), with allow-all and push+pr.

We want to test that PR is created and merged on that repo and feature is marked as completed in shep. 

For now, i want this wf to be MANUALLY triggered
summary: >
  I want to add CICD which runs one feature using shep with claude on real repository.
We have CLAUDE_CODE_OAUTH_TOKEN secret with claude token.
I want single cicd wf that clones https://github.com/shep-ai/sheep repo, we have RELEASE_TOKEN with git secret with admin access to that repo.
After cloning repo i want to run "shep feat new" with some simple fast feature (in FAST MODE), with allow-all and push+pr.

We want to test that PR is created and merged on that repo and feature is marked as completed in shep. 

For now, i want this wf to be MANUALLY triggered
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures:
  []

technologies:
  []

relatedLinks:
  []

# Open questions (must be resolved before implementation)
openQuestions:
  []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  I want to add CICD which runs one feature using shep with claude on real repository.
We have CLAUDE_CODE_OAUTH_TOKEN secret with claude token.
I want single cicd wf that clones https://github.com/shep-ai/sheep repo, we have RELEASE_TOKEN with git secret with admin access to that repo.
After cloning repo i want to run "shep feat new" with some simple fast feature (in FAST MODE), with allow-all and push+pr.

We want to test that PR is created and merged on that repo and feature is marked as completed in shep. 

For now, i want this wf to be MANUALLY triggered

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
