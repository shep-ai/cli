# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: feature-soft-delete
number: 056
branch: feat/056-feature-soft-delete
oneLiner: >
  We have delay in feature deletion process, it probably takes some time but our UI do optimistic deletion then updated and reappeared, total chaos.
  I want to fix the root cause, i want to add Deleting status to feature, while delting ui shows appropriate icon etc, deleting will do evertyhing including cleanup and delete the feature completly.
  We should do soft delete to feature in Db, put the feature in Deleted state and NOT show in any place. Only wiht explicit flag show deleted (both for ui and cli)
userQuery: >
  We have delay in feature deletion process, it probably takes some time but our UI do optimistic deletion then updated and reappeared, total chaos.
  I want to fix the root cause, i want to add Deleting status to feature, while delting ui shows appropriate icon etc, deleting will do evertyhing including cleanup and delete the feature completly.
  We should do soft delete to feature in Db, put the feature in Deleted state and NOT show in any place. Only wiht explicit flag show deleted (both for ui and cli)
summary: >
  We have delay in feature deletion process, it probably takes some time but our UI do optimistic deletion then updated and reappeared, total chaos.
  I want to fix the root cause, i want to add Deleting status to feature, while delting ui shows appropriate icon etc, deleting will do evertyhing including cleanup and delete the feature completly.
  We should do soft delete to feature in Db, put the feature in Deleted state and NOT show in any place. Only wiht explicit flag show deleted (both for ui and cli)
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures: []

technologies: []

relatedLinks: []

# Open questions (must be resolved before implementation)
openQuestions: []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  We have delay in feature deletion process, it probably takes some time but our UI do optimistic deletion then updated and reappeared, total chaos.
  I want to fix the root cause, i want to add Deleting status to feature, while delting ui shows appropriate icon etc, deleting will do evertyhing including cleanup and delete the feature completly.
  We should do soft delete to feature in Db, put the feature in Deleted state and NOT show in any place. Only wiht explicit flag show deleted (both for ui and cli)

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
