# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: review-resume-from-error
number: 067
branch: feat/067-review-resume-from-error
oneLiner: Currently when feature is in error/failed step, meaning not running and not awaiting approve - in that case we show one of review tabs with reject message, now if we try to send it, we will get error that feature not in awaiting approve step, we need to fix it, if any reject/approve sent via prompt in erorr state - we resume AND provide the user comment in first lines of prompt!
userQuery: >
  Currently when feature is in error/failed step, meaning not running and not awaiting approve - in that case we show one of review tabs with reject message, now if we try to send it, we will get error that feature not in awaiting approve step, we need to fix it, if any reject/approve sent via prompt in erorr state - we resume AND provide the user comment in first lines of prompt!
summary: >
  Currently when feature is in error/failed step, meaning not running and not awaiting approve - in that case we show one of review tabs with reject message, now if we try to send it, we will get error that feature not in awaiting approve step, we need to fix it, if any reject/approve sent via prompt in erorr state - we resume AND provide the user comment in first lines of prompt!
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures: []

technologies: []

relatedLinks: []

# Open questions (must be resolved before implementation)
openQuestions: []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Currently when feature is in error/failed step, meaning not running and not awaiting approve - in that case we show one of review tabs with reject message, now if we try to send it, we will get error that feature not in awaiting approve step, we need to fix it, if any reject/approve sent via prompt in erorr state - we resume AND provide the user comment in first lines of prompt!

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
