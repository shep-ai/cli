# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: feature-branch-dependencies
number: 084
branch: feat/084-feature-branch-dependencies
oneLiner: >
  Allow dragging/moving feature to be depended on another feature,
  add support to auto rebase when the other feature finishes its implementation cycle.
userQuery: >
  Allow dragging/moving feature to be depended on another feature,
  add support to auto rebase when the other feature finishes its implementation cycle.
summary: >
  Allow dragging/moving feature to be depended on another feature,
  add support to auto rebase when the other feature finishes its implementation cycle.
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures:
  []

technologies:
  []

relatedLinks:
  []

# Open questions (must be resolved before implementation)
openQuestions:
  []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Allow dragging/moving feature to be depended on another feature,
  add support to auto rebase when the other feature finishes its implementation cycle.

  ## Success Criteria

  - [ ] TBD

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | TBD  | TBD    | TBD       |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - To be refined during research

  ---

  _Generated by feature agent — proceed with research_
