# Feature Specification (YAML)
# This is the source of truth. Markdown is auto-generated from this file.

name: inline-devserver-icon
number: 069
branch: feat/069-inline-devserver-icon
oneLiner: >
  Inline the devserver browser icon in the feature node top row alongside
  the agent icon instead of adding a separate row that causes card overflow
userQuery: >
  Enabling dev server causes card size to change and cards to overflow over
  each other. Instead of generating a new line on the feature node, put the
  browser icon in the same line as the agent icon (top left) and make it
  clickable.
summary: >
  Move the deployment status indicator from a separate row to an inline
  clickable icon in the top-right corner of the feature node card, alongside
  fast-mode and agent icons. This prevents card size changes and overflow
  when the dev server is enabled.
phase: Analysis
sizeEstimate: M

# Relationships
relatedFeatures: []

technologies: []

relatedLinks: []

# Open questions (must be resolved before implementation)
openQuestions: []

# Markdown content (the actual spec)
content: |
  ## Problem Statement

  Enabling dev server causes card size to change and cards to overflow over
  each other. Instead of generating a new line on the feature node, put the
  browser icon in the same line as the agent icon (top left) and make it
  clickable.

  ## Success Criteria

  - [x] Deployment icon rendered inline in feature node top row
  - [x] Icon is clickable and opens dev server URL in new tab
  - [x] No card size change when deployment is active
  - [x] Storybook stories updated
  - [x] Unit tests passing

  ## Affected Areas

  | Area | Impact | Reasoning |
  | ---- | ------ | --------- |
  | feature-node.tsx | High | Main component change |
  | feature-node.stories.tsx | Medium | New story for combined icons |
  | feature-node.test.tsx | Medium | Updated tests for inline behavior |

  ## Dependencies

  None identified.

  ## Size Estimate

  **M** - Small UI refactor

  ---

  _Generated by feature agent — proceed with research_
